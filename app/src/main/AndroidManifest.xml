<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.contest.vezdekod">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vezdekod">
        <activity
            android:name=".GroupBrowser"
            android:exported="true"
            android:label="@string/title_activity_group_browser"
            android:theme="@style/Theme.Vezdekod.NoActionBar">
        </activity>
        <activity
            android:name=".LoggedActivity"
            android:exported="true"
            android:label="@string/title_activity_logged"
            android:theme="@style/Theme.Vezdekod.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Vezdekod.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>